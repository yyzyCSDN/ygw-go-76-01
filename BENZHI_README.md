基于 Go 实现的自动售检票闸机联控系统项目，一款车站通行控制服务，完成票务校验、闸机开合控制、通道计数、客流统计与设备心跳监控。

## 功能

系统覆盖车站闸机联控的完整链路：乘客刷票后按票种规则与进出站规则完成票务校验，
校验通过则驱动闸机开合；闸机开合由传感器确认并带超时回退；每台闸机独立状态机，
后台按批轮询设备心跳并统计通道客流，异常设备进入告警与恢复流程。

## 构建

```bash
go build -mod=vendor ./...
```

## 运行

```bash
go run -mod=vendor ./cmd/afc -listen 127.0.0.1:8080 -data ./data
```

启动后访问 http://127.0.0.1:8080/ 打开联控台，访问 /api/health 做健康探测。

## 测试

```bash
go test -mod=vendor ./...
go vet -mod=vendor ./...
```

## 依赖

唯一直接依赖 github.com/cespare/xxhash/v2（已 vendor 离线），用于票号去重键与
闸机事件指纹计算。
